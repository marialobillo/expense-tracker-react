import React from 'react';

const Header = ({title}) => {
    return (
        <header className="">
            <h1 className="text-center">{title}</h1>
        </header>
    );
}

export default Header;